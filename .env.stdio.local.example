# 本機 stdio 最小設定。
# 複製為 .env 後，只填 PingCode enterprise Client Credentials。
# stdio 不支援 user_oauth，也不需要 Redis、callback、公開 URL 或 OAuth Store。

PINGCODE_AUTH_MODE=enterprise
PINGCODE_CLIENT_ID=replace-with-enterprise-client-id
PINGCODE_CLIENT_SECRET=replace-with-enterprise-client-secret
